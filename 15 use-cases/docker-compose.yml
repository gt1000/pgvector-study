
---

# 📌 3. Docker Compose

  ```yaml
version: "3.8"

services:
  pgvector:
    image: your-pgvector-image
    container_name: pgvector
    ports:
      - "35432:5432"
    environment:
      POSTGRES_USER: study_postgres
      POSTGRES_PASSWORD: study_postgres
      POSTGRES_DB: study

  embedding-service:
    build:
      context: ./embedding-service
      dockerfile: Dockerfile
    container_name: embedding-service
    ports:
      - "8000:8000"
    depends_on:
      - pgvector

  llm-service:
    build:
      context: ./llm-service
      dockerfile: Dockerfile
    container_name: llm-service
    ports:
      - "8001:8001"
    depends_on:
      - pgvector
